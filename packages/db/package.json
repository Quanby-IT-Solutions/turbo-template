{
	"name": "@repo/db",
	"version": "0.0.1",
	"description": "Framework-agnostic Drizzle ORM database package with schema definitions and database client.",
	"author": "",
	"private": true,
	"license": "UNLICENSED",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		"./schema": {
			"types": "./dist/schema.d.ts",
			"default": "./dist/schema.js"
		},
		"./client": {
			"types": "./dist/client.d.ts",
			"default": "./dist/client.js"
		}
	},
	"typesVersions": {
		"*": {
			"schema": [
				"./dist/schema.d.ts"
			],
			"client": [
				"./dist/client.d.ts"
			]
		}
	},
	"scripts": {
		"prebuild:contracts": "pnpm --filter @repo/contracts build",
		"build": "pnpm prebuild:contracts && tsc",
		"dev": "tsc --watch",
		"format": "prettier --check \"src/**/*.ts\"",
		"format:fix": "prettier --write \"src/**/*.ts\"",
		"lint": "eslint --flag unstable_native_nodejs_ts_config \"src/**/*.ts\" --fix",
		"typecheck": "pnpm prebuild:contracts && tsc --noEmit",
		"test:cov": "vitest run --coverage",
		"push": "pnpm build && drizzle-kit push",
		"studio": "pnpm build && drizzle-kit studio",
		"generate": "pnpm build && drizzle-kit generate",
		"migrate": "pnpm build && drizzle-kit migrate",
		"seed": "pnpm --filter @repo/contracts build && pnpm push && tsx src/seed.ts",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"@repo/contracts": "workspace:*",
		"drizzle-orm": "catalog:",
		"pg": "catalog:"
	},
	"devDependencies": {
		"@repo/eslint-config": "workspace:*",
		"@repo/prettier-config": "workspace:*",
		"@repo/tsconfig": "workspace:*",
		"@types/node": "catalog:",
		"@types/pg": "catalog:",
		"@vitest/coverage-v8": "^4.1.2",
		"dotenv": "catalog:",
		"drizzle-kit": "1.0.0-beta.9-e89174b",
		"eslint": "catalog:",
		"prettier": "catalog:",
		"tsx": "catalog:",
		"typescript": "catalog:",
		"vitest": "^4.1.2"
	},
	"prettier": "@repo/prettier-config"
}
